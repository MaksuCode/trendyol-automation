package pages.categorypages;

import org.openqa.selenium.WebDriver;

/**
 * The reason for creating an apart category page class is the future possibility of changes on the specified category page.
 */

public class WatchAndAccessoriesCategoryPage extends BaseCategory {
    public WatchAndAccessoriesCategoryPage(WebDriver driver) {
        super(driver);
    }
}
